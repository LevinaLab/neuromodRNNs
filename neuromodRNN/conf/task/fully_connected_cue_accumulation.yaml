# @package _global_
defaults:
  - /task_class/task/cue_accumulation@task
  - _self_
 
# ---------------------------------------------------------------------------
# Standard cue_accumulation experiment configuration.
# ---------------------------------------------------------------------------
net_arch:
  connectivity_rec_layer: full
  sparse_readout: False
  sparse_input: False 

# Network parameter overrides
net_params:
  k: 0.75
 
# Training parameter overrides
train_params:
  lr: 0.005
  iterations: 2000
  c_reg: 0.005